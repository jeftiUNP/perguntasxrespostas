datasource db {
  provider = "mysql"
  url      = env("DATABASE_URL_TEST")
}

generator client {
  provider = "prisma-client-js"
}

model User {
  id    Int    @id @default(autoincrement())
  name  String
  email String @unique
}